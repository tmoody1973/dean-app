# MOO-275 Maya browser acceptance — 2026-07-17

## Scope

This run verifies the complete Data to Decision hero journey only. It does not
exercise arbitrary data sources, a general BI product, either secondary track,
or failure-driven lesson rewriting owned by MOO-278.

## Environment

- Local Next.js application with the Eve Docker sandbox backend
- Fresh browser session: `dean-moo275`
- Eve session: `wrun_01KXR33B3J8AFX1Q9Q1ZJAXB3Q`
- Track: `data-to-decision`

## Fixed learner inputs

1. Outcome: `I need to decide which marketing campaigns deserve next quarter's budget.`
2. Anchor: `I am strong in spreadsheets and dashboards but new to SQL.`
3. Reality check: `I would compare total net return first: revenue minus spend, then check whether the ranking fits the budget decision.`

Maya asked exactly three calibration questions, one at a time. No workspace
write or lesson appeared before the third answer.

## Initial workspace compilation

The raw Eve stream recorded exactly seven initial `write_file` calls in this
order:

1. `/workspace/session.md` — `call_8bJBDKYrZQbO7tjrFDH064mo`
2. `/workspace/learner-profile.md` — `call_ddm5oT3nYFG7chYPX2PGV3Me`
3. `/workspace/curriculum.md` — `call_bCRzS4OLUK5pxohfEwNsbdGX`
4. `/workspace/lessons/01-question-framing.md` — `call_9kglzuPHv59RXaZ4pqlmmeAo`
5. `/workspace/lessons/02-sql-retrieval.md` — `call_v8RsTbgOqNPyrgemmqaAQA4k`
6. `/workspace/lessons/03-visualization-interpretation.md` — `call_MEf4yyNVwzywiNvdXOjxC3fo`
7. `/workspace/lessons/04-decision-ready-recommendation.md` — `call_FEJOk8xjYKQfl3OyQVFait4v`

Every generated file was inspected from its raw `write_file` input. The learner
profile preserved all three answers verbatim and derived a marketing-budget
work domain, core starting difficulty, and spreadsheet/dashboard analogy. The
curriculum contained exactly four ordered lessons. Each lesson carried the
canonical fixture, domain personalization, modality, difficulty, verification
tier, mastery requirement, and `onFailure` metadata.

The run exposed one unrelated metadata defect: Maya invented `2025-02-14` for
the session date. The hero instruction was hardened after the run to omit dates
unless the runtime supplies a trusted value and to never invent one. Dates are
not part of the MOO-275 learning or grading acceptance contract.

## Lesson evidence

### 1. Question framing

All four relationship pairs were first matched incorrectly. The UI reported
that the declared relationships were not satisfied and kept progression
locked. Correctly matching decision, criterion, population, and time window
then produced the explicit structural-pass state and enabled Done.

### 2. SQL retrieval

The starter query was submitted unchanged as a deliberate failure:

- Call: `call_5N88BYLyItHTW1wM22iMUfWf`
- Actual: `[["Search",3600],["Social",3000],["Email",2700]]`
- Expected: `[["Search",2400],["Email",2100],["Social",1500]]`
- Result: `passed: false`; progression remained locked and one hint appeared.

The corrected query calculated `revenue_dollars - spend_dollars` and ordered
the result descending:

- Call: `call_sKevsBGQ1qQ6cHzWclYnzUYg`
- Actual: `[["Search",2400],["Email",2100],["Social",1500]]`
- Expected: `[["Search",2400],["Email",2100],["Social",1500]]`
- Result: `passed: true`; the UI said the output was verified by execution and
  enabled Done.

### 3. Visualization interpretation

The lesson rendered a visible evidence hierarchy for Search `$2,400`, Email
`$2,100`, and Social `$1,500`. Correctly matching the decision criterion,
strongest evidence, comparison, and caveat satisfied the declared structural
relationships and enabled progression.

### 4. Decision-ready recommendation

Correctly matching recommendation, evidence, caveat, and next action satisfied
the declared structural relationships. Completing the lesson rewrote the
curriculum and session to `recommendation-artifact` / `awaiting-recommendation`
and requested exactly four labeled lines.

## Final artifact

The learner submitted:

```text
Action: Allocate the next budget increment to Search for the next-quarter campaign test.
Evidence: Search has the highest total net return at $2,400, ahead of Email at $2,100 and Social at $1,500.
Caveat: This snapshot does not prove the next dollar will perform the same or show longer-term customer quality.
Next action: Run a capped Search budget test, monitor the next campaign period, and reassess before scaling further.
```

Maya wrote `/workspace/artifacts/recommendation.md` in
`call_TRougsGFosWtoA7meQRufmAN`. The file was inspected in full. It contains:

- the learner's original budget decision;
- the separately sourced canonical business question;
- the separately sourced Search, Email, and Social evidence;
- the required action/evidence/caveat/next-action context; and
- the learner's four submitted lines unchanged under
  `## Learner recommendation (verbatim)`.

The final curriculum write set `current: complete`,
`recommendation_status: submitted`, and the artifact path. The final session
write set `phase: complete` and `state: complete`.

## Evidence collection commands

The browser UI was checked directly after each interaction. The raw evidence
was then compared with the UI by reading the session stream from index 0 and
filtering `actions.requested` and `action.result` events for `write_file` and
`grade_exercise`. This confirmed that the visible results matched the
authoritative Eve tool events.
