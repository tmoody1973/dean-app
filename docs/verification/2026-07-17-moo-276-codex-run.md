# MOO-276 Codex work-tool browser acceptance — 2026-07-17

## Scope

This run verifies the one approved Build a Work Tool with Codex lesson and its
prepared artifact. It does not verify a complete Codex curriculum, deployment,
arbitrary commands, or modification of an unrelated learner repository.

## Environment

- Local Next.js application with the Eve Docker sandbox backend
- Fresh local browser session: `dean-moo276`
- Eve session: `wrun_01KXR5R35CSCP0SBEJFTTWKV9C`
- Track: `build-work-tool-codex`

## Calibration

The learner selected `Build a Work Tool with Codex`. Dean asked exactly three
questions, one per turn, before writing any file:

1. Repetitive task: `Every Friday I turn spreadsheet task rows into a Markdown status update.`
2. Existing workflow: `I filter a CSV in a spreadsheet, group Done and Blocked rows, then paste the result into Slack.`
3. Desired result: `Given four fixed task rows, it reports the completed count and blocked task names, and repeatable tests pass.`

## Initial workspace compilation

The raw Eve stream recorded exactly seven initial `write_file` calls in this
order:

1. `/workspace/session.md` — `call_JYcjOuA4LAA93gyipWrVAGeG`
2. `/workspace/learner-profile.md` — `call_TjNciPg1qSNoPoNP4T7QnARA`
3. `/workspace/curriculum.md` — `call_sFvjAGgEKSFUR04l7LkQkuBv`
4. `/workspace/lessons/01-smallest-useful-build.md` — `call_vFlpDzyXQvszsWXvou728Lrg`
5. `/workspace/artifacts/codex-node-tool-v1/README.md` — `call_nuvp3rbjZ8dRm8wnvpfUQVOE`
6. `/workspace/artifacts/codex-node-tool-v1/src/index.mjs` — `call_knojeL2dZjYywN3ymqEqKBJJ`
7. `/workspace/artifacts/codex-node-tool-v1/tests/index.test.mjs` — `call_f3flw9SvFyFlJPkagzDUJXgm`

Every file was inspected from the authoritative `write_file` input. The profile
preserved all three answers verbatim. The course preview contained one active
lesson, one fixed artifact directory, and machine-verifiable status. The lesson
file carried the learner-domain example, Intent/Acceptance criteria/Verification/
smallest-build concepts, fixed profile, mastery, and `onFailure` metadata.

The generated project files contained:

- a README marker plus visible Intent, Acceptance criteria, Verification, and
  Smallest useful build sections;
- a dependency-free `formatStatus(items)` implementation that reports
  completed count and blocked task names; and
- the exact 726-byte server-owned behavioral test file with two meaningful
  fixtures.

No date was invented and no artifact file was written outside the approved
artifact directory.

## Lesson and browser verification

The single `codex-work-tool-01` module rendered three blocks:

1. A four-part reveal sequence for Intent, Acceptance criteria, Verification,
   and Smallest useful build.
2. A concept diagram connecting task rows, `formatStatus`, status summary, and
   behavioral tests.
3. A non-editable artifact-verification card exposing four fixed criteria.

Done remained locked until the artifact card received an authoritative pass.
The browser sent one fixed artifact request:

- Call: `call_YODqU0LvOm3uV8EfoobH9Kdj`
- Kind: `artifact`
- Profile: `codex-node-tool-v1`
- Module: `codex-work-tool-01`
- Block index: `2`

The authoritative result was `grader: artifact`, `passed: true`, and
`error: null`. Its four recorded checks were:

- README marker: present, 775 bytes.
- Exact prepared test contract: exact match, 726 bytes.
- Fixed Node syntax command: exit 0.
- Fixed Node behavioral test command: exit 0.

The browser displayed `Verified. Every fixed artifact criterion passed.` and
then enabled Done. No model-authored prose supplied or overrode `passed`.

## Learner explanation

After Done, Dean updated the course and session to await exactly two labeled
lines. The learner submitted:

```text
Change: The tool replaces my manual spreadsheet grouping with a repeatable status summary that counts completed work and names blocked tasks.
Verification: The fixed profile verified the README marker, exact behavioral tests, Node syntax, and both Node behavior tests.
```

Dean preserved this text verbatim in
`/workspace/artifacts/codex-node-tool-v1/LEARNER-EXPLANATION.md` through
`call_JOFuESCVNbS46B9cjwMB8Xma`, then marked the curriculum and session complete.

## Independent pass/fail check

The generated source and exact tests were copied to an isolated temporary
directory and run directly with the supported Node 24 runtime:

```text
node --check src/index.mjs
node --test tests/index.test.mjs
```

The untouched artifact passed 2/2 tests. The implementation was then changed
only so the completed count always returned zero. Syntax still passed, while
the same behavioral command failed 0/2 with actual values `Completed: 0/4`
and `Completed: 0/2`. Restoring the generated implementation returned the same
command to 2/2 passing.

The artifact grader regression also exercises a valid-syntax semantic break
through the same fixed profile and requires the authoritative result to fail
with `NONZERO_EXIT`.
